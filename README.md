# React Custom Hooks Lab: Form Storage

A small React app for a repair company's service form that persists user input between page refreshes using a custom `useLocalStorage` hook backed by the browser's local storage.

## Getting Started

Install dependencies:

```sh
npm install
```

Run the app in development mode:

```sh
npm run dev
```

Run the test suite:

```sh
npm run test
```
